extends layout

block content
	+quoteListPageHeader(quotes, limit, offset, sort, paginationBaseUrl)
		+pageTitle(`${quotes.length.toLocaleString()} Quote${quotes.length != 1 ? "s" : ""} in`)
			span.badge.text-bg-primary
				i.bi-list.me-2
				| #{list.name}

	- var offset = 0;
	- var quoteCount = quotes.length;

	include ./shared/quotes-list.pug



	