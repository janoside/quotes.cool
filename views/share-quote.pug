extends layout-share

block headContent
	title Quote...

block content

	.center-col
		if (list)
			h5.mb-3
				a.badge.text-bg-primary(href=`/list/${list._id}`)
					i.bi-list.me-1
					| #{list.name}
				
				|  &raquo;


		.lead
			+quoteBody(quote, {shareLayout:true, hideTags:true})